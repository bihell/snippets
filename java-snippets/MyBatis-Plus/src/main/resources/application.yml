spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bihell?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
  jpa:
    show-sql: true
logging:
  level:
    root: warn
    com.bihell.mp.mapper: trace
  pattern:
    console: '%p%m%n'
mybatis-plus:
  mapper-locations:
    - classpath:/mapper/*Mapper.xml